import { styled } from '@mui/material';
import { TableRow } from '@mui/material';

const TableRowWithBorder = styled(TableRow)(({ theme }) => ({
  borderTop: '5px solid #082032',
}));

export default TableRowWithBorder;
